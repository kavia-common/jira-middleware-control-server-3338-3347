# Application environment
APP_ENV=development
LOG_LEVEL=INFO

# CORS: comma-separated list, or leave empty to allow "*"
# Example: http://localhost:3000,https://myapp.example.com
APP_CORS_ORIGINS=

# API keys: comma-separated list of accepted API keys.
# Example: super-secret-1,another-key-2
APP_API_KEYS=change-me-strong-key

# JIRA connection (Use either JIRA_BASE_URL or JIRA_CLOUD_SITE)
# If using Cloud site, the base URL will be built as: https://<JIRA_CLOUD_SITE>.atlassian.net
JIRA_BASE_URL=
JIRA_CLOUD_SITE=

# JIRA credentials
# Email should be the Atlassian account email for the API token
JIRA_EMAIL=
JIRA_API_TOKEN=

# JIRA client behavior
# HTTP timeout in seconds per request
JIRA_TIMEOUT_SECONDS=15
# Max retry attempts for transient errors (429/5xx) and HTTP errors
JIRA_RETRY_MAX_ATTEMPTS=3
# Base backoff (seconds) for exponential retries
JIRA_RETRY_BACKOFF_BASE=0.5

# Custom field display names (must match your JIRA instance)
# These are resolved to customfield_xxxxx ids at runtime.
JIRA_STORY_POINTS_FIELD_NAME=Story points
JIRA_EPIC_LINK_FIELD_NAME=Epic Link
JIRA_EPIC_NAME_FIELD_NAME=Epic Name

# Defaults
# Timezone for interpreting sprint start/end strings if used by clients
DEFAULT_TIMEZONE=UTC
# Optional default Agile board id; leave blank if not applicable
DEFAULT_BOARD_ID=
